package GC

import (

)

const (
	SYNCVAR_REGISTRY = 				byte(0)
	SYNCVAR_REGISTRY_RESPONSE = 	byte(1)
	SYNCVAR_UPDATE = 				byte(2)
)

const (
	INT64SYNCED = 		byte(0)
	FLOAT64SYNCED = 	byte(1)
	STRINGSYNCED = 		byte(2)
)